# CLIKCA 시스템 인터렉션 아키텍처

## 📋 개요

본 문서는 CLIKCA 시스템의 시스템 아키텍처, AI Agent 구조와 각 에이전트 간의 상호작용, 데이터 흐름도, 도구 사용 패턴, 그리고 전체적인 워크플로우를 상세히 분석한 아키텍처 다이어그램입니다.

---

## 🏗️ 시스템 구성

### 1. 시스템 아키텍처

```mermaid
graph TB
    %% CLIENT LAYER
    subgraph "CLIENT LAYER"
        DESKTOP[Desktop App<br/>Electron + React<br/>설치형 배포]
    end

    %% GATEWAY LAYER
    subgraph "GATEWAY LAYER"
        NGINX[Nginx<br/>Reverse Proxy]
    end

    %% APPLICATION LAYER
    subgraph "APPLICATION LAYER"
        subgraph "FastAPI Backend"
            DOC_API[Document Routes<br/>/api/v1/files/*]
            CHAT_API[Chat Routes<br/>/api/v1/chat/*]
            AUTH_API[Auth Routes<br/>/api/v1/auth/*]
            CAL_API[Calendar Routes<br/>/api/v1/calendar/*]
            USER_API[User Routes<br/>/api/v1/users/*]
        end
        
        subgraph "AI Agent Layer"
            AI_AGENT_ROUTER[Master Routing Agent<br/>GPT-4o 기반 의도 분류]
        end
        
    end

    %% DATA PROCESSING LAYER (OpenAI text-embedding-3-large)
    subgraph "DATA PROCESSING LAYER"
        DATA_SERVICE[Data Processing Service]
        MD_GEN[Markdown Generator<br/>Custom]
        VECTOR_EMB[Vector Embedder<br/>OpenAI text-embedding-3-large]
    end

    %% DATABASE LAYER
    subgraph "DATABASE LAYER"
        subgraph "AWS S3"
            S3_RAW[Raw Files<br/>/uploads/<br/>PDF, HWP]
            S3_PROCESSED[Processed Files<br/>/processed/<br/>Markdown]
        end

        MYSQL[(MySQL 8.0<br/>Primary DB<br/>- 사용자 정보<br/>- 채팅 기록<br/>- 파일 메타데이터<br/>- 일정 정보)]
        
        CHROMADB[(ChromaDB 0.4+<br/>Vector DB<br/>- 문서 벡터<br/>- 메타데이터<br/>- 검색 인덱스)]
    end


    %% 연결 관계
    DESKTOP --> NGINX

    NGINX --> AUTH_API
    NGINX --> CHAT_API
    NGINX --> DOC_API
    NGINX --> CAL_API
    NGINX --> USER_API

    %% API to Router
    CHAT_API --> AI_AGENT_ROUTER

    %% Document Processing Flow
    DOC_API --> DATA_SERVICE
    DATA_SERVICE --> MD_GEN
    MD_GEN --> VECTOR_EMB


    %% Database Connections
    AUTH_API --> MYSQL
    CHAT_API --> MYSQL
    DOC_API --> MYSQL
    CAL_API --> MYSQL
    USER_API --> MYSQL

    %% Storage Connections
    DOC_API --> S3_RAW
    MD_GEN --> S3_PROCESSED
    VECTOR_EMB --> CHROMADB

    %% 스타일링
    classDef clientLayer fill:#e1f5fe
    classDef proxyLayer fill:#f3e5f5
    classDef appLayer fill:#e8f5e8
    classDef routingLayer fill:#fff9c4
    classDef dataLayer fill:#fff3e0
    classDef storageLayer fill:#f1f8e9

    class DESKTOP clientLayer
    class NGINX proxyLayer
    class AUTH_API,CHAT_API,DOC_API,CAL_API,USER_API appLayer
    class LLM_ROUTER routingLayer
    class CHAT_AGENT,DOC_EDITOR_AGENT,DOC_SEARCH_AGENT appLayer
    class DATA_SERVICE,MD_GEN,VECTOR_EMB dataLayer
    class MYSQL,CHROMADB,S3_RAW,S3_PROCESSED storageLayer

```

### 2. AI Agent Router 상세 시스템 아키텍처

```mermaid
graph TB

    %% API 계층
    subgraph "BAXKEND LAYER"
        CHAT_ROUTER[Chat Routes<br/>/api/v1/chat/*]
    end

    %% AI Agent 라우팅 계층
    subgraph "AI AGENT LAYER (LangGraph)"
        MASTER_ROUTER[Master Routing Agent<br/>GPT-4o 기반 의도 분류]
    end

    %% 전문 에이전트 계층
    subgraph "AI AGENT LAYER"
        subgraph "GeneralChatAgent"
            GENERAL_LLM[GPT-4o<br/>일반 대화 처리]
            GENERAL_MEMORY[Memory Saver<br/>대화 기록 관리]
        end
        
        subgraph "DocumentSearchAgent"
            SEARCH_LLM[GPT-4o<br/>검색 쿼리 처리]
            SEARCH_TOOLS[Search Tools<br/>RAG + 확장 + 요약]
            SEARCH_MEMORY[Memory Saver<br/>대화 기록 관리]
        end
        
        subgraph "DocumentEditorAgent"
            EDITOR_LLM[GPT-4o<br/>문서 편집 처리]
            EDITOR_TOOLS[Editor Tools<br/>HTML 편집 도구들]
            EDITOR_MEMORY[Memory Saver<br/>대화 기록 관리]
        end
    end

    %% 도구 계층 (Tools Layer)
    subgraph "TOOLS LAYER (LangChain)"
        subgraph "Search Tools"
            RAG_TOOL[RAG Search Tool<br/>벡터 검색]
            QUERY_EXPAND[Query Expansion<br/>검색어 확장]
            SUMMARIZE[Summarization<br/>결과 요약]
            PRESIGNED_URL[Presigned URL<br/>다운로드 링크 생성]
        end
        
        subgraph "Editor Tools"
            HTML_EDITOR[HTML Editor<br/>TipTap 호환 편집]
            TEXT_REPLACE[Text Replace<br/>텍스트 치환]
            DOC_TEMPLATE[Document Templates<br/>보고서/회의록 템플릿]
            STYLE_APPLY[Style Applier<br/>스타일 적용]
        end
    end

    %% 데이터 계층
    subgraph "DATABASE LAYER"
        subgraph "벡터 데이터베이스"
            CHROMADB[(ChromaDB<br/>OpenAI Embeddings<br/>text-embedding-3-large)]
        end
        
        subgraph "관계형 데이터베이스"
            MYSQL[(MySQL<br/>채팅 기록<br/>사용자 정보<br/>문서 메타데이터)]
        end
        
        subgraph "파일 저장소"
            S3[(AWS S3<br/>문서 파일<br/>마크다운 파일)]
        end
    end

    %% 연결 관계 정의
    
    CHAT_ROUTER --> MASTER_ROUTER

    
    %% 라우팅 결과에 따른 에이전트 선택
    MASTER_ROUTER --> GENERAL_LLM
    MASTER_ROUTER --> SEARCH_LLM
    MASTER_ROUTER --> EDITOR_LLM
    
    %% 에이전트와 메모리/도구 연결
    GENERAL_LLM --> GENERAL_MEMORY
    
    SEARCH_LLM --> SEARCH_TOOLS
    SEARCH_LLM --> SEARCH_MEMORY
    SEARCH_TOOLS --> RAG_TOOL
    SEARCH_TOOLS --> QUERY_EXPAND
    SEARCH_TOOLS --> SUMMARIZE
    SEARCH_TOOLS --> PRESIGNED_URL
    
    EDITOR_LLM --> EDITOR_TOOLS
    EDITOR_LLM --> EDITOR_MEMORY
    EDITOR_TOOLS --> HTML_EDITOR
    EDITOR_TOOLS --> TEXT_REPLACE
    EDITOR_TOOLS --> DOC_TEMPLATE
    EDITOR_TOOLS --> STYLE_APPLY
    
    %% 도구들과 데이터 소스 연결
    RAG_TOOL --> CHROMADB
    PRESIGNED_URL --> S3
    
    %% 모든 에이전트는 MySQL에 대화 기록 저장
    GENERAL_LLM --> MYSQL
    SEARCH_LLM --> MYSQL
    EDITOR_LLM --> MYSQL
    
    %% 스타일링

    classDef apiLayer fill:#e8f5e8
    classDef routingLayer fill:#fff9c4
    classDef agentLayer fill:#f3e5f5
    classDef toolLayer fill:#fff3e0
    classDef dataLayer fill:#fce4ec

    class CHAT_ROUTER,DOC_ROUTER apiLayer
    class MASTER_ROUTER,INTENT_ANALYZER,DOC_CHECKER routingLayer
    class GENERAL_LLM,SEARCH_LLM,EDITOR_LLM agentLayer
    class RAG_TOOL,QUERY_EXPAND,SUMMARIZE,PRESIGNED_URL,HTML_EDITOR,TEXT_REPLACE,DOC_TEMPLATE,STYLE_APPLY toolLayer
    class CHROMADB,MYSQL,S3 dataLayer
```

### 3. DocumentSearchAgent 워크플로우

```mermaid
graph TD
    A[검색 요청 수신] --> B[Query Expansion<br/>검색어 확장]
    B --> C[RAG Search Tool<br/>벡터 검색 수행]
    C --> D[ChromaDB에서<br/>관련 문서 검색]
    D --> E[검색 결과 획득]
    E --> F[Summarization Tool<br/>결과 요약 및 답변 생성]
    F --> G{다운로드 요청?}
    G -->|Yes| H[Presigned URL Tool<br/>다운로드 링크 생성]
    G -->|No| I[최종 답변 반환]
    H --> I

    classDef searchProcess fill:#e3f2fd
    classDef toolProcess fill:#fff9c4
    classDef dataProcess fill:#fce4ec

    class A,E,I searchProcess
    class B,C,F,H toolProcess
    class D dataProcess
```

### 4. DocumentEditorAgent 워크플로우

```mermaid
graph TD
    A[편집 요청 수신] --> B[문서 내용 분석]
    B --> C{편집 유형 판단}
    
    C -->|단순 텍스트 교체| D[Text Replace Tool]
    C -->|HTML 구조 편집| E[HTML Editor Tool]
    C -->|템플릿 생성| F[Document Template Tool]
    C -->|스타일 적용| G[Style Applier Tool]
    
    D --> H[편집 완료]
    E --> H
    F --> H
    G --> H
    
    H --> I[문서 상태 업데이트]
    I --> J[최종 확인 메시지 생성]

    classDef editProcess fill:#e8f5e8
    classDef toolProcess fill:#fff9c4

    class A,B,H,I,J editProcess
    class D,E,F,G toolProcess
```

### 5. Sequence diagram

#### 1. 문서 처리, 인증

```mermaid
sequenceDiagram
    participant C as Desktop App<br/>(Electron)
    participant N as Nginx
    participant API as FastAPI
    participant R as LLM Router<br/>(OpenAI GPT-4o)
    participant CA as Chat Agent
    participant DE as Document Editor
    participant DS as Document Search
    participant DB as MySQL
    participant V as ChromaDB
    participant S3 as AWS S3

    %% 문서 업로드 및 처리 플로우
    Note over C,S3: 📄 문서 업로드 및 처리 플로우
    
    C->>+N: 1. 업로드 URL 요청
    N->>+API: 2. 요청 전달
    API->>+S3: 3. Presigned URL 생성
    S3-->>-API: 4. 업로드 URL 반환
    API-->>-N: 5. 업로드 URL 응답
    N-->>-C: 6. URL 전달
    
    C->>+S3: 7. 파일 직접 업로드
    S3-->>-C: 8. 업로드 완료
    
    C->>+N: 9. 파일 메타데이터 저장
    N->>+API: 10. 요청 전달
    API->>+DB: 11. 파일 정보 저장
    DB-->>-API: 12. 저장 완료
    API-->>-N: 13. 처리 완료 응답
    N-->>-C: 14. 완료 알림
    
    Note over API,S3: 📊 백그라운드 문서 처리 (OpenAI GPT-4o Embeddings)
    API->>+S3: 15. 원본 파일 다운로드
    S3-->>-API: 16. 파일 데이터
    
    API->>API: 17. PDF/HWP → Markdown 변환
    
    API->>+S3: 18. 처리된 파일 업로드
    S3-->>-API: 19. 업로드 완료
    
    API->>API: 20. 텍스트 임베딩 생성 (OpenAI Embeddings)
    
    API->>+V: 21. 벡터 데이터 저장
    V-->>-API: 22. 인덱싱 완료


    %% 인증 플로우
    Note over C,DB: 🔐 사용자 인증 플로우
    
    C->>+N: 39. 로그인 요청
    N->>+API: 40. 요청 전달
    API->>+DB: 41. 사용자 인증 확인
    DB-->>-API: 42. 인증 결과
    
    API->>API: 43. JWT 토큰 생성
    API-->>-N: 44. 토큰 응답
    N-->>-C: 45. 토큰 전달
```

#### 2. 채팅 

```mermaid
sequenceDiagram
    participant C as Desktop App<br/>(Electron)
    participant N as Nginx
    participant API as FastAPI
    participant R as LLM Router<br/>(OpenAI GPT-4o)
    participant CA as Chat Agent
    participant DE as Document Editor
    participant DS as Document Search
    participant DB as MySQL
    participant V as ChromaDB
    participant S3 as AWS S3

    %% 채팅 질의응답 플로우
    Note over C,V: 💬 AI 채팅 질의응답 플로우 (OpenAI GPT-4o)
    
    C->>+N: 23. 채팅 메시지 전송
    N->>+API: 24. 요청 전달
    
    API->>+DB: 25. 사용자 메시지 저장
    DB-->>-API: 26. 저장 완료
    
    API->>+R: 27. LLM 라우터 호출
    R->>R: 28. 질의 분석 (OpenAI GPT-4o)
    
    alt 일반 대화
        R->>+CA: 29a. Chat Agent 호출
        CA->>CA: 30a. 대화 처리 (OpenAI GPT-4o)
        CA-->>-R: 31a. 응답 반환
    else 문서 편집
        R->>+DE: 29b. Document Editor 호출
        DE->>DE: 30b. 문서 편집 (OpenAI GPT-4o)
        DE-->>-R: 31b. 편집 결과 반환
    else 문서 검색
        R->>+DS: 29c. Document Search 호출
        DS->>+V: 30c. 관련 문서 검색
        V-->>-DS: 31c. 검색 결과
        DS->>DS: 32c. RAG 처리 (OpenAI GPT-4o)
        DS-->>-R: 33c. 최종 응답
    end
    
    R-->>-API: 34. 처리 완료
    
    API->>+DB: 35. AI 응답 저장
    DB-->>-API: 36. 저장 완료
    
    API-->>-N: 37. 스트리밍 응답
    N-->>-C: 38. 실시간 응답 전송
```

---

## 🛠️ 도구(Tools) 상세 분석

### 검색 관련 도구들

| 도구명 | 기능 | 입력 | 출력 |
|--------|------|------|------|
| **RAG Search Tool** | ChromaDB 벡터 검색 | 키워드 리스트 | 관련 문서 청크 |
| **Query Expansion** | 검색어 확장 및 개선 | 원본 쿼리 | 확장된 쿼리들 |
| **Route Query** | 후속질문 vs 신규검색 판단 | 대화 상태 | 라우팅 결정 |
| **Handle Follow Up** | 후속 질문 처리 | 대화 상태 | 상태 유지 |
| **Summarization** | 검색 결과 요약 | 문서 + 쿼리 | 최종 답변 |
| **Presigned URL** | S3 다운로드 링크 생성 | 파일 키 | 다운로드 URL |

### 편집 관련 도구들

| 도구명 | 기능 | TipTap 호환성 |
|--------|------|---------------|
| **HTML Editor** | 구조적 HTML 편집 | ✅ 완전 호환 |
| **Text Replace** | 단순 텍스트 치환 | ✅ 호환 |
| **Document Templates** | 보고서/회의록 템플릿 | ✅ 호환 |
| **Formatted Text** | 스타일 적용 (굵게, 이탤릭 등) | ✅ 호환 |
| **List Creator** | 순서/비순서 리스트 생성 | ✅ 호환 |
| **Table Creator** | 테이블 생성 및 스타일링 | ✅ 호환 |
| **Blockquote** | 인용문/들여쓰기 | ✅ 호환 |
| **Style Applier** | CSS 스타일 적용 | ✅ 호환 |

---

## 🔧 상태 관리 (AgentState)

```typescript
interface AgentState {
    prompt: string                    // 사용자 입력
    document_content?: string         // 편집 대상 문서
    messages: BaseMessage[]           // 대화 기록 (LangGraph 관리)
    intent: string                   // 라우팅 결과
    needs_document_content: boolean   // 문서 요청 필요 여부
    intermediate_steps: any[]         // 중간 처리 결과
    generation?: string               // 최종 생성 답변
}
```

### 상태 흐름

1. **초기 상태**: 사용자 메시지와 기본 설정
2. **라우팅 단계**: `intent` 필드에 라우팅 결과 저장
3. **문서 처리**: `document_content` 필드 업데이트
4. **도구 실행**: `intermediate_steps`에 중간 결과 누적
5. **최종 응답**: `generation` 필드에 최종 답변 저장

---

## 📈 확장성 고려사항

### 1. 새로운 에이전트 추가
- **라우팅 로직** 수정으로 새 에이전트 통합 가능
- **플러그인 구조**: 독립적인 에이전트 개발

### 2. 도구 확장
- **LangChain Tool** 인터페이스 준수
- **타입 안정성**: Pydantic 모델 활용

### 3. 다중 LLM 지원
- **추상화 계층**: LLM 교체 용이
- **모델별 최적화**: 용도에 따른 모델 선택

